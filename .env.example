# Sao chép thành .env và điền key thật (KHÔNG commit .env)
OPENAI_API_KEY=sk-...
# tuỳ chọn override
# VNGR_LLM_MODEL=gpt-4o
# VNGR_ARTIFACTS_DIR=artifacts
# VNGR_API_URL=http://localhost:8000   # để Gradio gọi qua FastAPI thay vì in-process
